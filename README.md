# 🏛️ Stonerose Frontend

Современный Next.js frontend для каталога памятников и ритуальных принадлежностей.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

### Сборка для продакшена
```bash
npm run build
npm start
```

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env.local`:

```env
# API Configuration
NEXT_PUBLIC_API_URL=http://193.47.42.105/api
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

### API Endpoints

Frontend взаимодействует с отдельным Express.js backend через API:

- **Памятники**: `GET /api/monuments`
- **Товары**: `GET /api/products`
- **Ограды**: `GET /api/fences`
- **Аксессуары**: `GET /api/accessories`
- **Ландшафт**: `GET /api/landscape`

## 📦 Структура проекта

```
frontend/
├── src/
│   ├── app/                    # Next.js 13+ App Router
│   │   ├── components/         # Переиспользуемые компоненты
│   │   ├── monuments/          # Страницы памятников
│   │   ├── fences/            # Страницы оград
│   │   ├── accessories/       # Страницы аксессуаров
│   │   ├── admin/             # Панель администрирования
│   │   └── ...
│   └── lib/
│       ├── api-client.ts      # Клиент для API запросов
│       └── ...
├── public/                    # Статические файлы
├── package.json
└── README.md
```

## 🛠️ Технологический стек

- **Framework**: Next.js 15 с App Router
- **UI**: React 19 + TailwindCSS 4
- **Language**: TypeScript
- **Bundler**: Turbopack (для ускорения разработки)
- **API**: Fetch API с кастомным клиентом

## 🔗 API Integration

Frontend использует централизованный API клиент (`src/lib/api-client.ts`) для всех запросов к backend:

```typescript
import { apiClient, API_ENDPOINTS } from '../lib/api-client';

// Получение памятников
const monuments = await apiClient.get(API_ENDPOINTS.monuments);

// Создание нового товара
const newProduct = await apiClient.post(API_ENDPOINTS.products, data);
```

## 📱 Адаптивность

- ✅ Мобильные устройства (320px+)
- ✅ Планшеты (768px+)
- ✅ Десктоп (1024px+)

## 🎯 Основные функции

- 📋 Каталог памятников с фильтрацией
- 🔍 Поиск по товарам
- 🛒 Карточки товаров с 360° обзором
- 📊 Панель администратора
- 📝 Блог и новости
- 📞 Контактная информация

## 🚢 Деплой

Frontend готов к деплою на любой платформе поддерживающей Next.js:

- Vercel (рекомендуется)
- Netlify
- AWS Amplify
- Railway
- Или собственный сервер

## 🔧 Разработка

### Добавление новых страниц

1. Создайте папку в `src/app/`
2. Добавьте `page.tsx` файл
3. Используйте API клиент для получения данных

### Добавление новых компонентов

1. Создайте файл в `src/app/components/`
2. Экспортируйте компонент как default
3. Импортируйте где необходимо

## 📊 Production URLs

- **Frontend**: http://localhost:3000 (development)
- **API Backend**: http://193.47.42.105/api
- **Health Check**: http://193.47.42.105/health

---

Разработано с ❤️ для Stonerose