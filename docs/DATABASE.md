# 🏗️ Строй Дом - База данных и админ-панель

## Структура базы данных

База данных PostgreSQL с тремя основными таблицами:

- **categories** - Категории товаров
- **subcategories** - Подкатегории (привязаны к категориям)
- **products** - Товары (привязаны к категориям и подкатегориям)

## Настройка базы данных

### 1. Создайте PostgreSQL базу данных

Рекомендуемые сервисы (бесплатный уровень):
- [Neon](https://neon.tech) - serverless PostgreSQL
- [Supabase](https://supabase.com) - PostgreSQL с дополнительными функциями
- [Vercel Postgres](https://vercel.com/storage/postgres) - интеграция с Vercel

### 2. Настройте переменные окружения

```bash
# Скопируйте пример
cp .env.example .env.local

# Отредактируйте .env.local и добавьте строку подключения
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"
```

### 3. Создайте таблицы в базе данных

```bash
# Сгенерировать миграции на основе схемы
npm run db:generate

# Применить миграции (создать таблицы)
npm run db:push
```

### 4. Перенесите данные из products.ts

```bash
# Запустить скрипт миграции данных
npm run db:migrate-data
```

Скрипт перенесёт:
- 16 категорий из `menuCategories.ts`
- ~60 подкатегорий
- ~200 товаров из `products.ts`

## Команды

| Команда | Описание |
|---------|----------|
| `npm run db:generate` | Генерация SQL-миграций из схемы |
| `npm run db:push` | Применение схемы к базе данных |
| `npm run db:studio` | Открыть Drizzle Studio (веб-интерфейс БД) |
| `npm run db:migrate-data` | Перенос данных из mock-файлов |

## Админ-панель

После настройки БД админ-панель доступна по адресу:

```
http://localhost:3000/admin
```

### Разделы админ-панели:

- `/admin` - Главная страница с инструкцией
- `/admin/products` - Управление товарами (CRUD)
- `/admin/categories` - Управление категориями и подкатегориями

### Функции админ-панели:

✅ Просмотр, создание, редактирование, удаление категорий
✅ Просмотр, создание, редактирование, удаление подкатегорий
✅ Просмотр, создание, редактирование, удаление товаров
✅ Поиск и фильтрация товаров
✅ Пагинация
✅ Автоматическая транслитерация slug

## API Endpoints

### Категории
- `GET /api/admin/categories` - Список категорий с подкатегориями
- `POST /api/admin/categories` - Создать категорию
- `PUT /api/admin/categories` - Обновить категорию
- `DELETE /api/admin/categories?id=1` - Удалить категорию

### Подкатегории
- `GET /api/admin/subcategories` - Список подкатегорий
- `GET /api/admin/subcategories?categoryId=1` - Подкатегории категории
- `POST /api/admin/subcategories` - Создать подкатегорию
- `PUT /api/admin/subcategories` - Обновить подкатегорию
- `DELETE /api/admin/subcategories?id=1` - Удалить подкатегорию

### Товары
- `GET /api/admin/products` - Список товаров с пагинацией
- `GET /api/admin/products?categoryId=1&search=профнастил&page=1` - С фильтрами
- `POST /api/admin/products` - Создать товар
- `PUT /api/admin/products` - Обновить товар
- `DELETE /api/admin/products?id=1` - Удалить товар

## Структура файлов

```
src/
├── lib/
│   └── db/
│       ├── schema.ts     # Схема таблиц Drizzle
│       └── index.ts      # Подключение к БД
├── app/
│   ├── admin/
│   │   ├── layout.tsx    # Layout админ-панели
│   │   ├── page.tsx      # Главная админки
│   │   ├── products/
│   │   │   └── page.tsx  # Управление товарами
│   │   └── categories/
│   │       └── page.tsx  # Управление категориями
│   └── api/
│       └── admin/
│           ├── categories/route.ts
│           ├── subcategories/route.ts
│           └── products/route.ts
scripts/
└── migrate-data.ts       # Скрипт миграции данных
drizzle.config.ts         # Конфигурация Drizzle Kit
```

## Деплой на Vercel

1. Создайте базу данных (Neon/Supabase)
2. Добавьте `DATABASE_URL` в Environment Variables на Vercel
3. После первого деплоя выполните миграции:
   ```bash
   npm run db:push
   npm run db:migrate-data
   ```

## Troubleshooting

### Ошибка "DATABASE_URL is not set"
Убедитесь, что файл `.env.local` существует и содержит `DATABASE_URL`.

### Ошибка подключения к базе данных
- Проверьте формат строки подключения
- Убедитесь, что база данных доступна (не в режиме сна)
- Для Neon добавьте `?sslmode=require` в конец URL

### Товары не отображаются в админке
1. Проверьте, что таблицы созданы: `npm run db:push`
2. Проверьте, что данные мигрированы: `npm run db:migrate-data`
3. Откройте Drizzle Studio: `npm run db:studio`
