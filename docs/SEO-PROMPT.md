# Промпт для нового чата: SEO-оптимизация сайта "Строй Дом"

---

## 🎯 Задача

Выполнить полную SEO-оптимизацию сайта stroydom30.ru согласно плану в файле `docs/SEO-OPTIMIZATION-PLAN.md`. Сайт — магазин строительных материалов в Астрахани.

---

## 📁 Информация о проекте

**Технологии:**
- Next.js 16.1.1 (App Router) с Turbopack
- TypeScript
- PostgreSQL + Drizzle ORM
- Tailwind CSS
- Деплой: PM2 на сервере 178.250.157.34

**Структура:**
- Путь: `c:\new_stroydom\New-stroy-dom`
- Сервер: `/var/www/new-stroy-dom`
- Домен: https://stroydom30.ru
- Процесс PM2: `stroy-dom`

**Статистика:**
- 269 товаров в `src/app/mock/products.ts`
- 16 категорий, ~50 подкатегорий
- 6 статей в блоге (нужно 15-20)
- 260 изображений (все WebP)

**Контакты магазина:**
- Телефон: 8-937-133-33-66
- Email: info@stroydom30.ru
- Магазин №1: ул. Рыбинская, 25Н (Пн-Сб: 08:00-16:00, Вск: 08:00-14:00)
- Магазин №2: пл. Свободы, 14К (Пн-Сб: 09:00-19:00, Вск: 10:00-18:00)

---

## ✅ Что уже реализовано (НЕ ТРОГАТЬ)

1. **Open Graph теги** — есть в `layout.tsx`, `page.tsx`, страницах товаров
2. **Twitter Cards** — есть в layout.tsx
3. **JSON-LD Product** — есть в `src/app/catalog/[category]/[subcategory]/[product]/page.tsx`
4. **JSON-LD BreadcrumbList** — есть там же
5. **sitemap.xml** — динамический в `src/app/sitemap.ts`
6. **robots.txt** — в `src/app/robots.ts`
7. **Canonical URL** — только на страницах товаров
8. **Все изображения** — уже WebP, alt-теги используют product.title

---

## 📋 Задачи для выполнения (ПО ПОРЯДКУ)

### Этап 1: Schema.org разметка

**1.1 Создать файл `src/app/components/SchemaOrg.tsx`**

Компоненты для JSON-LD:
- `LocalBusinessSchema` — информация о магазине (2 адреса, часы работы)
- `OrganizationSchema` — организация
- `WebSiteSchema` — сайт с SearchAction
- `ItemListSchema` — для страниц категорий (список товаров)
- `ArticleSchema` — для статей блога

Использовать данные магазина из секции "Контакты магазина" выше.

**1.2 Добавить в `src/app/layout.tsx`:**
- Импортировать и добавить `<OrganizationSchema />` и `<WebSiteSchema />` в начало body

**1.3 Добавить в `src/app/page.tsx`:**
- Импортировать и добавить `<LocalBusinessSchema />` в начало компонента Home

**1.4 Добавить в `src/app/contacts/page.tsx`:**
- Импортировать и добавить `<LocalBusinessSchema />`
- ВНИМАНИЕ: это client component ('use client'), нужно учесть это

---

### Этап 2: Canonical URL на всех страницах

Добавить `alternates: { canonical: '...' }` в metadata/generateMetadata:

| Файл | Canonical URL |
|------|---------------|
| `src/app/catalog/page.tsx` | `https://stroydom30.ru/catalog` |
| `src/app/catalog/[category]/page.tsx` | `https://stroydom30.ru/catalog/${categorySlug}` |
| `src/app/catalog/[category]/[subcategory]/page.tsx` | `https://stroydom30.ru/catalog/${categorySlug}/${subcategorySlug}` |
| `src/app/blog/page.tsx` | `https://stroydom30.ru/blog` |
| `src/app/blog/[slug]/page.tsx` | `https://stroydom30.ru/blog/${slug}` |
| `src/app/contacts/page.tsx` | `https://stroydom30.ru/contacts` (создать metadata) |
| `src/app/sales/page.tsx` | `https://stroydom30.ru/sales` |

---

### Этап 3: Обновить sitemap.ts

В файле `src/app/sitemap.ts`:

1. Раскомментировать/добавить секцию с товарами:
```typescript
// Импортировать getCategorySlug, getSubcategorySlug из mock/products
const productUrls = products.map(product => ({
  url: `${BASE_URL}/catalog/${getCategorySlug(product.mainCategory)}/${getSubcategorySlug(product.subCategory)}/${product.urlId}`,
  lastModified: currentDate,
  changeFrequency: 'weekly' as const,
  priority: 0.6,
}));
```

2. Добавить блог (получить посты из БД или захардкодить slugs)

3. Добавить в return: `...productUrls, ...blogUrls`

---

### Этап 4: ItemList Schema для категорий

**4.1 В `src/app/catalog/[category]/page.tsx`:**
- Добавить `<ItemListSchema>` с подкатегориями как items

**4.2 В `src/app/catalog/[category]/[subcategory]/page.tsx`:**
- Добавить `<ItemListSchema>` с товарами как items

---

### Этап 5: Article Schema для блога

В `src/app/blog/[slug]/page.tsx`:
- Добавить `<ArticleSchema>` с данными поста (title, excerpt, publishedAt, image)

---

### Этап 6: Написать новые статьи блога

Добавить в seed через `src/app/api/admin/blog/seed/route.ts` или через админку:

| № | Slug | Тема | Категория |
|---|------|------|-----------|
| 7 | kak-vybrat-profnastil-dlya-zabora | Как выбрать профнастил для забора в Астрахани | советы |
| 8 | shtukaturka-sten-poshagovaya-instrukciya | Штукатурка стен: пошаговая инструкция | инструкции |
| 9 | uteplenie-doma-minvatoy | Утепление дома минватой: советы экспертов | советы |
| 10 | kak-klast-plitku-gayd | Как класть плитку: полный гайд для новичков | инструкции |
| 11 | vidy-gidroizolyacii-dlya-vannoy | Виды гидроизоляции для ванной комнаты | обзоры |
| 12 | remont-kvartiry-s-chego-nachat | Ремонт квартиры в Астрахани: с чего начать | советы |
| 13 | sravnenie-brendov-knauf-ceresit-volma | Сравнение брендов: Knauf, Ceresit, Волма | обзоры |
| 14 | rashod-materialov-kalkulyator | Расход материалов: как рассчитать правильно | инструкции |

Статьи должны:
- Содержать 1500-2500 символов
- Включать ключевые слова для Астрахани
- Иметь внутренние ссылки на товары (relatedProductIds)
- Быть полезными для покупателей

---

## 🚀 Деплой

После каждого этапа или в конце:

```bash
# Билд локально
npm run build

# Деплой на сервер
ssh root@178.250.157.34 "cd /var/www/new-stroy-dom && git pull && npm run build && pm2 restart stroy-dom"
```

---

## ⚠️ Важные замечания

1. **НЕ менять** существующую структуру — только добавлять
2. **Проверять типы** — проект на TypeScript
3. **contacts/page.tsx** — это client component, Schema нужно добавить правильно
4. **Тестировать** после изменений через `npm run build`
5. **Коммитить** после каждого этапа с понятным сообщением

---

## 🔍 Проверка результата

После деплоя проверить:
1. https://search.google.com/test/rich-results — JSON-LD
2. https://validator.schema.org/ — Schema.org
3. View Source на сайте — canonical, OG теги

---

Начни с чтения файла `docs/SEO-OPTIMIZATION-PLAN.md` для полного контекста, затем выполняй задачи по порядку. Спрашивай если что-то непонятно!
